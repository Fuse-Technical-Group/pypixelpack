name: Governance Lint
on:
  push:
    branches: [main]
  pull_request:

jobs:
  governance:
    uses: repentsinner/symphonize/.github/workflows/governance-lint.yml@notation--v0
    with:
      readme-type: "library"
